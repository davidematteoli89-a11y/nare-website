import { notFound } from "next/navigation";

/** Dettaglio Narè Ritiri — skeleton di route (Fase 1B). Nessun contenuto/endpoint reale ancora esistente. */
export default function RitiriDetailPage() {
  notFound();
}
