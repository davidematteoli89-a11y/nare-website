import { notFound } from "next/navigation";

/**
 * Dettaglio Workshop — skeleton di route (Step 1B). L'endpoint pubblico
 * GET /api/public/[orgSlug]/workshops/[slug] esiste già in aiDady, ma il
 * collegamento reale è demandato alla Fase 2/successiva, quando esisteranno
 * Workshop pubblici pubblicati (oggi: 0).
 */
export default function WorkshopDetailPage() {
  notFound();
}
